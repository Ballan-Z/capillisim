"""Mosaic Estimator web app: drag an image, solve size <-> distance <-> caps.

FastAPI backend (`server.app`) + a static vanilla-JS frontend. Run with
``python -m cap_mosaic.app.webapp``. See docs/ESTIMATOR.md.
"""
